import { IndiqueGanhe } from "@/features/growth/IndiqueGanhe";

/** `/account/indicar` — página dedicada do Indique e Ganhe. */
export default function AccountIndicarPage() {
  return <IndiqueGanhe />;
}

# Movepark Hub — Specs

Documentação técnica de referência para o design do Movepark Hub.
Baseada em análise dos projetos legados `movepark-backoffice-v4` e `movepark-nextjs`.

## Índice

| Arquivo | Conteúdo |
|---|---|
| [domain-model.md](./domain-model.md) | Mapeamento de domínio: legado → Hub, glossário, entidades principais |
| [pricing-engine.md](./pricing-engine.md) | Motor de cálculo de preço dinâmico: padrões identificados, modelo de banco, regras transversais |
| [capacity-rules.md](./capacity-rules.md) | Regras de capacidade e controle de disponibilidade por data |
| [database-schema.md](./database-schema.md) | Visão geral do schema atual, decisões de modelagem, migrations existentes |
| [booking-flow.md](./booking-flow.md) | Ciclo de vida da reserva: state machine, sequência de checkout, expiração, cancelamento |
| [booking-modifications.md](./booking-modifications.md) | **Alterações de reserva gateadas pela Tarifa** (E2.8): cancelar (bloqueio fora da janela) + trocar data/veículo por benefício; verdade no servidor, override de staff/parceiro |
| [coupon-rules.md](./coupon-rules.md) | Motor de Cupons & Descontos — **pilar cupom** (código): validação, ciclo de uso, RPCs, RLS, UI, testes |
| [discount-rules.md](./discount-rules.md) | Motor de Cupons & Descontos — **pilar desconto automático** (regra sem código no `simulate_price`, alimenta `old_price`): modelo, avaliação, empilhamento com cupom, UI, testes |
| [reviews.md](./reviews.md) | Avaliações por estacionamento (PRD-08): modelo, agregado, coleta pós-estadia, exibição, resposta do dono, JSON-LD |
| [voucher-qrcode.md](./voucher-qrcode.md) | Geração de voucher PDF, check-in por QR code, notificações |
| [spot-guarantee.md](./spot-guarantee.md) | Garantia de vaga (PRD-14): promessa de plataforma, regra operacional, selo + acionamento (WhatsApp) |
| [partner-onboarding.md](./partner-onboarding.md) | Onboarding de parceiro em 2 etapas: captura de lead → aprovação manual → wizard de setup |
| [partner-onboarding-redesign.md](./partner-onboarding-redesign.md) | **Redesenho do wizard (E1.9):** fluxo rápido split-screen — publica com o mínimo (tipos+capacidade+preço de balcão, endereço geocodificado, destino auto-detectado, traslado) e "deixa redondo depois" no painel; remove CNPJ/KYC do wizard; preview travado. Decisões: Q-010, D-003, ADR-008 |
| [destinations.md](./destinations.md) | Destinos (aeroportos/etc): catálogo de busca + páginas de conteúdo SEO `/destinos/<slug>` + CRUD no Manager |
| [location-destination-proximity.md](./location-destination-proximity.md) | Vínculo `location.destination_id` + proximidade por **PostGIS** `ST_Distance` (view `location_proximity`, ADR-001) — fundação de PRD-09/PRD-13 |
| [destination-points.md](./destination-points.md) | Pontos do destino (terminais T1/T2/T3) + distância por terminal via **PostGIS** `ST_Distance` (view `location_point_proximity`) — granularidade por terminal de PRD-09/PRD-13 |
| [distance-display.md](./distance-display.md) | Exibição de distância/proximidade ao aeroporto e terminal (PRD-09): badge "mais perto do Tx" no card + lista por terminal no detalhe — consome DAT-04/DAT-05 |
| [public-api.md](./public-api.md) | **API pública** (E0.7): endpoints versionados `/v1` sobre as RPCs existentes, chaves de API com escopos, gestão no operator, proxy `api.movepark.co` (CF Worker), OpenAPI. Contém o **ADR-003** (doc-as-you-build). MCP = Fase 2 |
| [mcp.md](./mcp.md) | **MCP** (E0.7 Fase 2): servidor MCP in-repo (Edge `mcp`, Streamable HTTP) com duas superfícies — **consumidor** (`mcp.movepark.co`, anon) e **parceiro** (`/partner`, chave `mp_`+escopo) sobre a API v1; substitui o MCP n8n |
| [chatbot.md](./chatbot.md) | **Web chat do Hub** (E3.3): assistente ("bolinha") no consumidor, LLM **Gemini** (function-calling), transacional (reservar/cancelar) reusando Edges/RPCs; Edge `chat` (`verify_jwt=false`) + `ChatWidget`. MVP interno, só webchat |
| [payment-split.md](./payment-split.md) | **Gateway com split** (E0.1 · ADR-004): camada de vínculo parceiro ↔ recebedor gateway-agnóstica (`company_payout_account`/`payout_recipient`/`payout_recipient_event` + `take_rate_bps`), abstração `_shared/payments`, Edge `sync-recipient`. Cobrança PIX/cartão e webhooks = E0.1.2–.4 |
| [fares.md](./fares.md) | **Tarifas de flexibilidade** (E2.8 · Básica/Flex/Superflex): níveis vendidos no checkout (modelo aéreo), receita de serviço Movepark fora do split da vaga, janela de cancelamento por Tarifa (Superflex = 1 min). Núcleo financeiro (E2.8-e) implementado; front/admin pendentes |
| [movepark-wallet.md](./movepark-wallet.md) | **Carteira Movepark** (Motor de Crescimento): saldo em real (BRL, 1 para 1), ledger de cashback/indicação, crédito na conclusão da reserva, idempotência, expiração, RLS/hardening. Contém o **contrato de reversão** que cancelamento/reembolso/alteração de data vão respeitar quando o débito de saldo existir |

## Status

| Spec | Status |
|---|---|
| domain-model | ✅ Definido |
| pricing-engine | ✅ Analisado — migration aplicada (`20260526100000`) |
| capacity-rules | ✅ Implementado — migration `20260614000000`. Hold na criação + release; `cron_expire_pending_bookings` (pending abandonado → `cancelled`); enforce de `minimum_stay`/`minimum_date`/antecedência; `check_availability`/`availability_batch` (disponibilidade no listing/busca, "esgotado"+quase-lotação); `operator_location_occupancy` + tela `/operator/occupancy`. Ver [capacity-rules.md](./capacity-rules.md) |
| database-schema | ✅ Schema base + extensões aplicadas (`20260526100002`) |
| booking-flow | ✅ Definido |
| coupon-rules | ✅ Implementado (Fase 1 + Fase 2) — migration `20260611000000`, RPCs `operator_*_coupon`/`coupon_evaluate`/`validate_coupon` + trigger de incremento, painel `/operator/coupons`, cupom no listing + desconto no checkout, pgTAP `coupon_rpc.test.sql`. Ver [coupon-rules.md](./coupon-rules.md) |
| discount-rules | ✅ Implementado (Fase 1 + Fase 2) — migration `20260612000000`, `discount_evaluate` (best-pick) + RPCs `operator_*_discount`, `simulate_price`/`create_booking_atomic` aplicam o desconto + snapshot `booking_discount`, empilha cupom (`allow_coupon_stack`), aba Descontos em `/operator/coupons` ("Promoções"), selo no listing, pgTAP `discount_rpc.test.sql`. Ver [discount-rules.md](./discount-rules.md) |
| reviews | ✅ Implementado (Fase 1+2, PRD-08.1–08.6) — migration `20260613000000`, agregado + RPCs `submit_review`/`operator_respond_review`, coleta por e-mail (`review-request` + pg_cron), exibição (card/detalhe/bloco), JSON-LD AggregateRating/Review, painel `/operator/reviews`, **moderação `/manager/reviews` (08.5)** e **curadoria "Mais bem avaliados" no destino (08.6)**. Ver [reviews.md](./reviews.md) |
| voucher-qrcode | ✅ Implementado — migration `20260615000000` (bucket `vouchers`), edge `voucher-pdf` (PDF + signed URL), página `/voucher/validate` (check-in por QR do operador, confirmed→checked_in via RLS). Ver [voucher-qrcode.md](./voucher-qrcode.md) |
| storage-buckets (OPS-05) | ✅ Implementado — migration `20260619000000`, buckets `assets-public` (público + CDN/transform), `vouchers` (privado + signed URL) e `partner-uploads` (privado, RLS por `company_id`); `partner-assets` aposentado; pgTAP `storage_buckets.test.sql`. Ver [storage-buckets.md](./storage-buckets.md) |
| spot-guarantee (PRD-14) | ✅ Implementado (MVP, front-end) — selo "Vaga garantida" no listing (gateado por disponibilidade) + checkout, seção "Sobre a garantia", e acionamento por WhatsApp da unidade (fallback suporte) no detalhe da reserva. Sem migration. Ver [spot-guarantee.md](./spot-guarantee.md) |
| partner-onboarding | ✅ Implementado — migrations `20260603120000`–`20260603120400`, edge functions `submit-partner-lead`/`approve-partner`, UI Stage 1/Manager/Stage 2 |
| partner-onboarding-redesign (E1.9) | 🟢 Fase 1 "Publicar" implementada — migration `20260802000000` (`onboarding_publish` + `has_shuttle`), wizard split-screen, Google Places, preview travado. Gate D-003 resolvido (§9). Fase 2 (checklist + mover KYC) pendente. Ver [partner-onboarding-redesign.md](./partner-onboarding-redesign.md) |
| destinations | ✅ Implementado — migration `20260609120000`, página SSG `/destinos/<slug>`, CRUD `/manager/destinations`, menu "Destinos" no header |
| faq-camadas (GEO-07 · ADR-002) | ✅ Implementado (engenharia) — migration `20260621000000`, escopo `destination` + `faq.destination_id`, Edge `get-faq` mescla `global+destination+location` (dedupe), render em `/destinos/<slug>` e listing, aba **FAQ** no admin do destino + filtro de escopo no admin central, `FAQPage` único por página. **Pendente:** popular FAQs de aeroporto (`gestao/conteudo-onda1.md`) e **deploy** da Edge `get-faq`. Ver [destinations.md](./destinations.md) |
| location-destination-proximity | ✅ Implementado — migrations `20260616000000` + `20260618000000` (PostGIS, ADR-001), FK `location.destination_id` + backfill, coluna gerada `geog`+GiST, `nearest_destination`/`locations_proximity` (`ST_Distance`/`ST_DWithin`), trigger auto-fill, view `location_proximity`, override no `LocationForm`. Ver [location-destination-proximity.md](./location-destination-proximity.md) |
| destination-points | ✅ Implementado — migrations `20260617000000` + `20260618000000` (PostGIS), tabela `destination_point` (RLS espelha `destination`, coluna gerada `geog`+GiST), `nearest_destination_point`, view `location_point_proximity` (`is_nearest`) via `ST_Distance`, seed T1/T2/T3 do GRU, dialog "Terminais" no Manager. Ver [destination-points.md](./destination-points.md) |
| distance-display | ✅ Implementado (PRD-09) — Edge `search` devolve `location.nearest_terminal` via RPC PostGIS `locations_proximity` (sem geo em TS, ADR-001), badge "mais perto do Tx" no `ResultCard`, lista por terminal no detalhe via `useLocationTerminals`. Ver [distance-display.md](./distance-display.md) |
| operator add-ons | ✅ Implementado — migration `20260610000000`, RPCs `operator_upsert_addon`/`operator_set_location_addon`/`operator_delete_addon`, CRUD `/operator/addons` (ver [operator-panel.md](./operator-panel.md) §4.5) |
| como-chegar (PRD-11) | ✅ Implementado — migration `20260620000000`, colunas `location.directions_text` + traslado (`shuttle_frequency_minutes`/`shuttle_to_terminal_minutes`, CHECK > 0); `notice` vira aviso crítico de entrada; bloco "Como chegar" no detalhe (`HowToArrive` + `howToArrive.logic.ts`), campos no `LocationForm` (admin+operador), pgTAP `location_directions.test.sql`. Ver [listing-detail.md](./customer/listing-detail.md) §5.5 |
| public-api (E0.7) | 🚧 Fase 1 **no ar** (validada e2e) — migration `20260624000000` (`api_key`/`api_scope` + RPCs gestão/verify/`api_*` + reserva por parceiro) aplicada no projeto linkado; **Edge Function `api` deployada** (`verify_jwt=false`) e validada ponta a ponta (200 com dados tenant-scoped, 401 sem chave, 403 escopo errado); `public/openapi.yaml` + `api-catalog`/`llms.txt`, UI `/operator/api-keys`, drift-guard no CI. **worker `api.movepark.co` deployado** (custom domain, secret `SUPABASE_ANON_KEY` setado) e validado e2e pelo domínio (`/v1/*` só com `Bearer mp_…`, `/openapi.yaml`, `/docs`, 401 sem chave). ADR-003 ativo no CLAUDE.md. **Pendente:** `test:db` pgTAP (Docker). Ver [public-api.md](./public-api.md) |
| api-observabilidade (E0.7 Fase 1.1) | ✅ **No ar** (validado e2e) — migration `20260625000000` (`api_request_log` + RLS + `operator_api_usage` + prune cron 90d) aplicada; gateway REST e MCP parceiro **deployados** gravando log por request autenticado (service_role, `EdgeRuntime.waitUntil`); **rate-limit ativo** (worker + KV `API_RATELIMIT`, 60 req/min → `429`); **página de docs do MCP** em `mcp.movepark.co` (Accept: text/html). Validado: 59×`rest 200`+1×`rest 403`+1×`mcp 200` logados com latência, 6×`429` barrados na borda (sem log). pgTAP `api_request_log_rpc.test.sql`. **Pendente (opcional):** UI de uso no operator. Ver [public-api.md](./public-api.md) §6/§10 |
| api-paridade (E0.7) | ✅ **No ar** (validado e2e) — migration `20260626000000` (aditiva): 18 RPCs `api_*` espelhando `operator_*` keyed por `company_id` + escopos `discounts:*`/`addons:*`/`occupancy:read`. REST passou a **24 rotas** e MCP a **36 tools**: cupons/descontos/add-ons (CRUD), avaliações (listar/responder), ocupação (leitura), edição de unidade/tipo de vaga, e destinos no consumidor. Cards + OpenAPI + drift-guard atualizados. **Pendente (opcional):** pgTAP dedicado dos `api_*` (lógica coberta pelos pgTAP `operator_*`). Ver [public-api.md](./public-api.md) §7/§9 + [mcp.md](./mcp.md) |
| mcp (E0.7 Fase 2) | ✅ **No ar** (validado e2e) — Edge `mcp` (Deno, JSON-RPC/Streamable HTTP, `verify_jwt=false`) **deployada**; worker roteia **`mcp.movepark.co`** (`/` consumidor anon, `/partner` chave `mp_`+escopo, reusa `api_*`/`api_key_verify`). Validado: `initialize`/`tools/list`/`tools/call` consumidor; parceiro com `tools/list` filtrado por escopo, dados tenant-scoped, tool fora de escopo → `-32602`, sem chave → 401. `server-card.json`+`partner-card.json`+agent-skills/api-catalog/llms atualizados; 10 testes deno; drift-guard cobre tools↔cards. **Pendente:** repontar `.mcp.json` (n8n→in-repo) e aposentar o n8n. Ver [mcp.md](./mcp.md) |
| payment-split (E0.1.1 + E1.3 + E0.1.2 · ADR-004) | 🚧 Vínculo + KYC + **PIX com split** implementados — migrations `20260627000000`/`20260628000000`/`20260629000000` aplicadas; abstração `_shared/payments` (`PaymentGateway`/`PagarmeGateway`/`MockGateway`/`getGateway`) com recebedores (register_information PJ + prova de vida) **e cobrança PIX com split** (`createPixCharge`/`getCharge`/`buildSplit`); Edges `sync-recipient` (deployada), `create-pix-charge` e `pagarme-webhook` (Basic auth + idempotência → booking confirmed); checkout PIX real (`Step3Payment`), recebedor master configurável no Manager (Configurações → Pagamentos). **Pendente:** deploy das Edges `create-pix-charge`/`pagarme-webhook` + cadastro do webhook no painel Pagar.me; cartão (E0.1.3). Ver [payment-split.md](./payment-split.md) |
| shared-availability (E2.5.1 + E2.5.2) | ✅ **No ar** (validado e2e contra o WL Virapark) — config por empresa (`wl_domain`/`wl_tenant_key`/`wl_sync_enabled`) + mapa por lote (`wl_category_slug`/`wl_product_slug`) + RPC `wl_company_config`, cliente `_shared/wl`, Edge `wl-sync` (pull ao vivo na Ocupação + catálogo no Manager). **E2.5.2 (anti-overbooking real):** `external_booked_count` no `location_parking_availability`, `_create_booking_core` conta `booked+external` sob lock, `check_availability`/`availability_batch` somam o external; **outbox `wl_delivery` + Edge `wl-deliver`** (push Hub→WL confiável, retry/backoff, cron 1 min) via trigger reserve/release; **reconciliação `wl-reconcile`** (pull `sold_wl` → `external_booked_count` via RPC `wl_reconcile_apply`, cron 15 min) + `wl_reconcile_log`. Migrations `20260711000000`/`20260711010000`. Ver [shared-availability.md](./shared-availability.md) |
| permissions (E1.6 + E1.7 + E1.8 · ADR-005) | ✅ **No ar** — permissões por escopo + 4 papéis fixos (Dono/Gerente/Operação/Financeiro). `company_role` ganha `manager`/`finance`; catálogo `api_scope` ganha 7 escopos in-app + `assignable_to_api_key`; presets seedados em `company_role_scope`; helpers `member_has_scope`/`current_member_scopes`. Enforcement server-authoritative: cada `operator_*`/`payout_*`/`company_*` de escrita exige o escopo + policies UPDATE de `location`/`location_parking_type`; UI espelha via `useAuth().hasScope` (`<RequireScope>`, sidebar, ações). Convite por e-mail (E1.7) na Edge `invite-company-member` (gate `team:write`); Manager escolhe papel ao vincular. Migrations `20260712000000`/`20260713000000`/`20260714000000`. Ver [permissions.md](./permissions.md) |
| movepark-wallet (Motor de Crescimento) | ✅ **Crédito no ar + testado.** Saldo em real (BRL, 1 para 1). Ledger `wallet_ledger` (append-only, idempotente por `wallet_cashback_once`), cashback na conclusão (`tg_booking_completed_cashback`, por `cashback_bps` do nível) e indicação dos dois lados (`tg_booking_completed_referral`, R$25), leitura `get_my_wallet`. Hardening `20260818000000` (anon fora de `get_my_wallet`/`redeem_referral_code`). pgTAP `wallet.test.sql`. **Pendente (fase seguinte):** débito no checkout + reversão no cancelamento/estorno (ver §contrato de reversão). Ver [movepark-wallet.md](./movepark-wallet.md) |
| fares (E2.8-e · backend completo) | ✅ **No ar (backend)** — enum `fare_tier` + catálogo `fare` (seed Básica grátis / Flex R$12,90 / Superflex R$24,90, RLS leitura pública), colunas `booking.fare_*`, RPC `get_unit_fares`. `_create_booking_core` (+wrappers) recebe `p_fare_tier` e snapshota preço/janela; `create-pix-charge`/`create-card-charge` roteiam a Tarifa 100% pra Movepark (fora do split); `cancel-booking` usa `fare_cancel_until` (Superflex = 1 min). **Passo 2:** auto-extensão por atraso de voo (RPC `extend_booking_flight_delay` + Edge `extend-booking` + log `booking_fare_extension`) e notificações WhatsApp Flex+ (`_shared/whatsapp.ts`, disparo em `pagarme-webhook`/`extend-booking`, degrada sem template). Migrations `20260717000000`/`20260718000000`. **Pendente:** front E2.8-b/c/d, admin E2.8-f. Ver [fares.md](./fares.md) |

## Migrations

| Arquivo | Conteúdo |
|---|---|
| `20260525142111_init_movepark_hub_schema.sql` | Schema base completo |
| `20260525142247_harden_security_definer_functions.sql` | Hardening da função `handle_new_auth_user` |
| `20260525143531_seed_parking_type_catalog.sql` | Seed: catálogo de tipos de vaga |
| `20260525155000_seed_companies_locations.sql` | Seed: empresas, unidades, pivots de vaga |
| `20260526100000_add_pricing_engine.sql` | Motor de preço: `pricing_rule`, `pricing_tier`, `pricing_hourly_bracket` |
| `20260526100001_add_capacity_rules.sql` | Disponibilidade por data + colunas de config em `location` e `location_parking_type` |
| `20260526100002_extend_booking_schema.sql` | Colunas faltantes em `booking` (expires_at, UTM, voucher, check-in real) |
| `20260526100003_seed_pricing_rules.sql` | Seed: 17 `pricing_rule` + tiers para todas as empresas do seed inicial |
| `20260603120000_add_onboarding_status_and_company_onboarding.sql` | Enum `onboarding_status`, colunas `company.onboarding_status`/`logo_url`, `location.photos`, tabela `company_onboarding` |
| `20260603120100_onboarding_rls.sql` | RLS de `company_onboarding` + defesa em profundidade no `catalog_read_company` |
| `20260603120200_onboarding_rpcs.sql` | RPCs `submit_partner_lead` + writes do wizard + `onboarding_submit` (go-live) |
| `20260603120300_partner_assets_storage.sql` | Bucket `partner-assets` + policies de escrita por empresa. **Aposentado pelo OPS-05** (`20260619000000`): substituído por `assets-public`; policies removidas |
| `20260603120400_onboarding_harden.sql` | Hardening: `search_path`, revogação de PUBLIC nas RPCs, listing do bucket |
| `20260603130000_app_setting.sql` | Configurações globais (key/value) editáveis pelo hub_admin — remetente/caixa de e-mail dos parceiros |
| `20260609120000_destination_seo.sql` | `destination`: colunas SEO/conteúdo (`slug` único + backfill, `meta_title`/`meta_description`, `intro`, `hero_image_url`, `is_published`), índice e trigger de slug |
| `20260610000000_add_on_management.sql` | Serviços adicionais geridos pelo operator: coluna `add_on_service.sort_order` + índice; RPCs `SECURITY DEFINER` `operator_upsert_addon`/`operator_set_location_addon`/`operator_delete_addon` + guard `addon_assert_company_access` |
| `20260611000000_coupon_engine.sql` | Motor de cupons (Fase 1+2): colunas `coupon.sort_order`/`description`/`per_user_limit`/`min_amount`/`min_days`, tabela `coupon_parking_type`; `coupon_evaluate`, RPCs `operator_upsert_coupon`/`operator_set_coupon_active`/`operator_delete_coupon`, `validate_coupon`, guard `coupon_assert_company_access`, trigger `payment_bump_coupon` (incrementa `times_used`); refactor de `create_booking_atomic` p/ usar `coupon_evaluate` |
| `20260612000000_discount_engine.sql` | Motor de descontos automáticos (Fase 1+2): tabelas `discount_rule`/`discount_rule_parking_type`/`booking_discount`, `discount_evaluate` (best-pick + janela/`min_days`/`min_amount`/`advance_days`/tipo de vaga), RPCs `operator_upsert_discount`/`operator_set_discount_active`/`operator_delete_discount`, guard `discount_assert_company_access`; `simulate_price` aplica o desconto (preview, `base_price`/`old_price`/`discount` no retorno) e `create_booking_atomic` re-avalia (autoritativo) + snapshot + empilha cupom |
| `20260613000000_reviews_engine.sql` | Avaliações (PRD-08 Fase 1): agregado `location.review_avg`/`review_count` + trigger `review_bump_rating`, colunas `review.owner_response*` e `booking.review_request_sent_at`, RPCs `submit_review`/`operator_respond_review`, automação `cron_complete_bookings` (pg_cron) + edge `review-request` (coleta por e-mail) |
| `20260614000000_capacity_real.sql` | Capacidade real: `min_stay_satisfied`, `check_availability` + `availability_batch` (disponibilidade por período), `create_booking_atomic` passa a aplicar `minimum_stay`/`minimum_date`/antecedência, `cron_expire_pending_bookings` + pg_cron `expire-pending-bookings` (pending abandonado → `cancelled`, libera hold), `operator_location_occupancy` (ocupação por data, gateada por empresa). `simulate_price` intacto |
| `20260615000000_voucher_storage.sql` | Voucher PDF: cria o bucket **privado** `vouchers`. PDF gerado pela edge `voucher-pdf` (signed URL, `booking.voucher_url`); check-in por QR em `/voucher/validate` reusa RLS (sem RPC nova) |
| `20260616000000_location_destination_link.sql` | Vínculo location↔destination (DAT-04): coluna `location.destination_id` (FK→`destination`, `on delete set null`) + índice; `haversine_km` (IMMUTABLE) e `nearest_destination` (teto 100 km); trigger `location_set_destination_trg` (auto-fill no INSERT); backfill dos 17 lotes pelo destino mais próximo; view `location_proximity` (`security_invoker`, `distance_km` on-the-fly). **Substituído por PostGIS em `20260618000000`** |
| `20260617000000_destination_point.sql` | Pontos do destino (DAT-05): tabela `destination_point` (FK→`destination` `on delete cascade`, `type` text+CHECK, `unique(destination_id,name)`, RLS espelha `destination`); `nearest_destination_point`; view `location_point_proximity` (`security_invoker`, `distance_km`+`is_nearest`); seed T1/T2/T3 do GRU por `code`. **Distância migrada p/ PostGIS em `20260618000000`** |
| `20260618000000_geo_postgis.sql` | **Geo em PostGIS (ADR-001):** habilita `postgis`; colunas geradas `geog geography(Point,4326)` + índices GiST em `destination`/`location`/`destination_point`; recria `nearest_destination`/`nearest_destination_point` e as views `location_proximity`/`location_point_proximity` com `ST_Distance`/`ST_DWithin`; nova RPC `locations_proximity(p_lat,p_lng,p_destination_id)` (distância + terminal mais próximo, consumida pelo Edge `search`); **remove** `haversine_km` (cálculo de geo no frontend foi eliminado) |
| `20260619000000_storage_buckets_ops05.sql` | **OPS-05 — Storage:** cria `assets-public` (público, CDN/transform, escrita por prefixo `<company_id>/` ou `hub_admin`), reforça `vouchers` (privado + limites) e cria `partner-uploads` (privado, RLS por `company_id`); aposenta o bucket legado `partner-assets` (remove policies). Ver [storage-buckets.md](./storage-buckets.md) |
| `20260621000000_faq_destination_scope.sql` | **GEO-07 / ADR-002 — FAQ em camadas:** estende o enum `faq_scope` com `destination`; adiciona `faq.destination_id` (FK→`destination`, `on delete cascade`) + índice parcial; reescreve `faq_check` para os 3 escopos (global/destination/location, sem duplicar chaves). RLS inalterada (escrita `destination` = `hub_admin` via `faq_admin_all`; leitura pública via `faq_public_select`). Ver [destinations.md](./destinations.md) |
| `20260620000000_location_directions_prd11.sql` | **PRD-11 — Como chegar:** colunas aditivas em `location` — `directions_text` (passo-a-passo, markdown) + traslado estruturado `shuttle_frequency_minutes`/`shuttle_to_terminal_minutes` (CHECK > 0 quando informado). Geo/distância não entram (DAT-04). pgTAP `location_directions.test.sql`. Ver [listing-detail.md](./customer/listing-detail.md) §5.5 |
| `20260622000000_company_member_roles.sql` | **E1.6 — Sub-papéis por empresa:** enum `company_role` (`owner`/`operator`) + coluna `profile_company.role` (NOT NULL default `owner` → backfill preserva acesso); helpers `is_company_owner`/`current_owner_company_ids`; RPCs `SECURITY DEFINER` `company_list_members`/`company_set_member_role`/`company_remove_member` (owner-only, guarda de "último dono"). Não altera o enum `user_role`. pgTAP `company_members_rpc.test.sql`. Ver [operator-panel.md](./operator-panel.md) §2 e §4.10 |
| `20260624000000_public_api.sql` | **Public API (E0.7):** `api_scope` (catálogo seedado) + `api_key` (hash+prefixo, escopos, RLS só-leitura); RPCs de gestão `operator_*_api_key` (segredo gerado server-side, devolvido 1×), `api_key_verify` (gateway, service_role), `api_*` tenant-scoped (`api_list_locations`/`api_get_location`/`api_list_parking_types`/`api_list_bookings`/`api_get_booking`/`api_simulate_price`/`api_assert_lpt_company`); reserva por parceiro — `booking.profile_id` nullable + `customer_*`/`created_via_api_key_id`/`idempotency_key` + `CHECK booking_actor_check`, núcleo `_create_booking_core` reusado por `create_booking_atomic` (wrapper) e `api_create_booking`/`api_cancel_booking`/`api_checkin_booking`/`api_checkout_booking` |
| `20260627000000_payout_recipients.sql` | **Gateway com split (E0.1.1 · ADR-004):** `company.take_rate_bps` (comissão da Movepark, default global em `app_setting`); enum `payout_recipient_status` (`draft`/`pending`/`action_required`/`active`/`refused`/`suspended`); `company_payout_account` (banco/KYC, agnóstico, 1:1 com company); `payout_recipient` (registro no gateway: `external_recipient_id`/status/`kyc_url`/`requirements`, único por `(company,provider)`); `payout_recipient_event` (log append-only das respostas). RLS: hub_admin full, operator só SELECT do próprio, escrita só via service_role. Ver [payment-split.md](./payment-split.md) |
| `20260628000000_payout_kyc_fields.sql` | **KYC do recebedor (E1.3 · ADR-004):** `company_payout_account.kyc_details` (jsonb com o `register_information` PJ — endereço, telefone, faturamento, fundação, tipo societário, representante legal); RPC `onboarding_upsert_payout_account(uuid, jsonb)` (operador salva no passo "Recebimento" do wizard, guarda `onboarding_assert_editable` + bump do passo 6). Ver [payment-split.md](./payment-split.md) |
| `20260629000000_pix_charges.sql` | **Cobrança PIX com split (E0.1.2 · ADR-004):** colunas em `payment` (`method`, `pix_qr_code`, `pix_qr_code_url`, `expires_at`, `split` jsonb), `app_setting.pagarme_movepark_recipient_id` (recebedor master, editável no Manager) e `payment_webhook_event` (idempotência de webhook, RLS só service_role). Edges `create-pix-charge` (split via `take_rate_bps`) + `pagarme-webhook` (Basic auth, order.paid/charge.paid → booking confirmed). Ver [payment-split.md](./payment-split.md) |
| `20260717000000_fare_tiers.sql` | **Tarifas de flexibilidade (E2.8-e):** enum `fare_tier`; catálogo `fare` (preço/janela/benefícios por nível, RLS leitura pública + escrita hub_admin, seed Básica/Flex/Superflex); colunas `booking.fare_tier`/`fare_price_cents`/`fare_cancel_until`/`fare_benefits`; RPC `get_unit_fares`. `_create_booking_core`+wrappers recebem `p_fare_tier` (snapshot de preço/janela). Tarifa = receita Movepark fora do split. Ver [fares.md](./fares.md) |
| `20260718000000_fare_flight_extension.sql` | **Auto-extensão por atraso de voo (E2.8-e passo 2):** RPC `extend_booking_flight_delay` (gate Superflex via `fare_benefits.flight_delay_protection`, estende `check_out_at` sem cobrança, re-segura capacidade das datas adicionadas) + log `booking_fare_extension` (RLS: dono/hub_admin leem, escrita service_role). Edge `extend-booking` + notificações WhatsApp Flex+ (`_shared/whatsapp.ts`). Ver [fares.md](./fares.md) |
| `20260720000000_fare_upgrade.sql` | **Upgrade de Tarifa pós-reserva (E2.8-d):** `payment.kind` (`booking`/`fare_upgrade`) + `payment.fare_target_tier`; RPC `apply_fare_upgrade` (promove a Tarifa — preço/janela/benefícios + delta no total, idempotente, sem downgrade). Edge `create-fare-upgrade` (cobra o delta via PIX, split 100% Movepark) + ramo `fare_upgrade` no `pagarme-webhook`. Ver [fares.md](./fares.md) |
| `20260721000000_location_fare.sql` | **Config de Tarifa por unidade (E2.8-f Frente A):** tabela `location_fare` (`enabled` + `price_cents_override` por unidade, RLS leitura pública + escrita hub_admin); `get_unit_fares` faz overlay; `_create_booking_core` usa o preço/on-off efetivo da unidade; RPC `operator_set_unit_fare` (gate `pricing:write`). Ver [fares.md](./fares.md) |
| `20260722000000_change_booking_dates.sql` | **Alterar datas (E2.8-f Frente B):** RPC `change_booking_dates` (reserva pendente — re-segura capacidade do novo período, re-precifica, recalcula janela da Tarifa, dropa cupom; reserva paga é recusada). Edge `change-booking-dates`. Ver [fares.md](./fares.md) |
| `20260727000000_high_demand_signal.sql` | **Sinal de demanda "Muito procurado hoje" (E3.6, recorte):** RPC `locations_high_demand_today` (SECURITY DEFINER, devolve só `location_id` que cruzaram o limiar — nunca a contagem, mesmo padrão de `popular_locations`) + `app_setting.high_demand_min_bookings_today` (default 3). Conta só `confirmed`/`checked_in`/`completed`/`no_show` de hoje (dia civil na timezone da location). Ver [search-results.md](./customer/search-results.md) §10 |
| `20260728000000_high_demand_signal_location_filter.sql` | **Fix de segurança (revisão pós-deploy):** `locations_high_demand_today` não filtrava `location.status`/`deleted_at` — SECURITY DEFINER contorna a RLS `catalog_read_location`, então uma location inativa/excluída ainda vazava o sinal pra quem já soubesse o `location_id`. Adiciona `l.status = 'active' and l.deleted_at is null`, igual `popular_locations`. |
| `20260729000000_account_anonymization.sql` | **Exclusão de conta + anonimização (E0.9 · LGPD art. 18):** RPC `anonymize_own_account()` (SECURITY DEFINER, opera sobre `auth.uid()`) — scrub da PII de `profiles`/`booking` (mantém a venda), hard-delete de `vehicle`/`address`/`payment_method`/`profile_saved`, guarda "só consumidor" (aborta se `profile_company`). Orquestrada pela Edge `delete-account` (cancela/estorna reservas ativas, apaga vouchers, scrub+ban do `auth.users`). pgTAP `account_deletion.test.sql`. Ver [account-deletion.md](./customer/account-deletion.md) |
| `20260730000000_identity_unification_prep.sql` + `..._identity_attach_otp.sql` + `..._identity_drop_profiles_phone.sql` | **Identidade unificada (E0.10 · ADR-006):** `auth.users` = fonte única da credencial; `profiles` perde `phone`. `merge_accounts` (reaponta FKs + dedupe + precedência + `account_merge_log`), `get_my_identities`, `identifier_otp` (OTP custom) + `find_user_by_identifier`/`account_has_history`/`merge_preview`; triggers e `anonymize_own_account` deixam de tocar `profiles.phone`. Edge `attach-identifier` (anexar-verificado + merge Q-006). pgTAP `account_merge.test.sql`. Ver [identity-unification.md](./customer/identity-unification.md) |
| `20260731000000_coupon_public_validation.sql` | **Cupom sem login (campanhas):** RPC `validate_coupon_public` (SECURITY DEFINER, grant `anon`) — preview do cupom sem sessão (`profile_id` NULL → pula `per_user_limit`; enforcement no `create_booking_atomic`), mesmo perfil read-only do `simulate_price`. Front: `?cupom=`/`?coupon=` lido/persistido (`src/lib/coupon.ts`) e pré-aplicado no `ReservationCard` sem login. pgTAP `coupon_public.test.sql`. Ver [coupon-rules.md](./coupon-rules.md) §9 |
| `20260806000000_booking_payer_snapshot.sql` | **Snapshot do pagador no booking + titular x passageiro:** `booking` ganha `customer_tax_id` (CPF/CNPJ do pagador, coletado no passo de pagamento) e `passenger_first_name/last_name/phone` (quem usa a vaga). O TITULAR (conta logada) é o pagador: `customer_*` = titular; o pagamento (PIX/cartão/fare-upgrade) passa a montar o pagador do **snapshot do booking**, não de `profiles`/`auth.users`. Edge nova `attach-phone-silent` grava o telefone no `auth.users` sem OTP com guarda de colisão (exceção da ADR-006, ver CLAUDE.md). CPF sai do gate de complete-profile e vira campo no passo de pagamento. pgTAP `booking_payer_snapshot.test.sql`. Ver [booking-flow.md](./booking-flow.md) |
| `20260805000000_booking_split_customer_name.sql` | **Nome do passageiro em `customer_first_name` / `customer_last_name`:** snapshot do pedido acompanha o checkout (Nome + Sobrenome). `customer_name` segue coluna real (contrato da Public API); trigger `booking_reconcile_customer_name` reconcilia os dois lados (quem manda customer_name tem o nome fatiado; quem manda first/last tem o customer_name recomposto), então nenhuma RPC/API muda. Checkout: campo "Nome do passageiro" vira Nome/Sobrenome. pgTAP `booking_customer_name_split.test.sql`. Ver [booking-flow.md](./booking-flow.md) |
| `20260804000001_profiles_split_name.sql` | **Nome do cliente em `first_name` / `last_name`:** `profiles` ganha `first_name`/`last_name`; `full_name` vira coluna **gerada** (`first + last`), então as leituras seguem sem toque e só a escrita muda. Helper `split_person_name` (usado no backfill e nos triggers). Triggers `handle_new_auth_user`/`handle_auth_user_updated`, `anonymize_own_account` e `merge_accounts` reescritos pra `first_name`/`last_name`. Front: forms `complete-profile`/`profile` viram dois campos; `session.firstName`/`lastName`; saudações por primeiro nome (conta + edges `review-request`/`pagarme-webhook`/`extend-booking`). pgTAP `profiles_name_split.test.sql`. Ver [identity-unification.md](./customer/identity-unification.md) |

## Pendências

| Item | Prioridade | Motivo |
|---|---|---|
| ~~**[BUG-001]** Corrigir seed Aerovalet Valet GRU~~ | ~~Alta~~ | ✅ Resolvido — `fix_aerovalet_valet_surcharge_seed` + `fix_aerovalet_valet_surcharge_source` |
| ~~Seed de capacidade real em `location_parking_type.capacity`~~ | ~~Alta~~ | ✅ Resolvido — seed já tem valores reais (15–1100); capacidade aplicada de ponta a ponta (`20260614000000`) |
| Decisão sobre modelo de staff/backoffice | Média | Necessário para políticas RLS de escrita |
| Preço dinâmico por janela/dia da semana/feriado | Baixa | Definido como v2, fora do MVP |
